#ifndef MAIN_WINDOW_HPP
#define MAIN_WINDOW_HPP

#include <SFML/Graphics.hpp>
#include <string>

class MainWindow : public sf::RenderWindow {
public:
    MainWindow();
    MainWindow(const std::string&, const float&, const float&);

    void run();
private:


};

#endif
